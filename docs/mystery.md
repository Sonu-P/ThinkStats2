## Module `code.mystery`

This file contains code used in "Think Stats",
by Allen B. Downey, available from greenteapress.com

Copyright 2014 Allen B. Downey
License: GNU GPLv3 http://www.gnu.org/licenses/gpl.html

### Import

```python
from code.mystery import *  # or import specific names
```

### Functions

- `expo_sample(n)`
```python
from code.mystery import expo_sample
# Example usage:
result = expo_sample(...)  # fill in arguments
```

- `gauss_sample(n)`
```python
from code.mystery import gauss_sample
# Example usage:
result = gauss_sample(...)  # fill in arguments
```

- `gumbel_sample(n)`
```python
from code.mystery import gumbel_sample
# Example usage:
result = gumbel_sample(...)  # fill in arguments
```

- `lognorm_sample(n)`
```python
from code.mystery import lognorm_sample
# Example usage:
result = lognorm_sample(...)  # fill in arguments
```

- `main()`
```python
from code.mystery import main
# Example usage:
result = main(...)  # fill in arguments
```

- `pareto_sample(n)`
```python
from code.mystery import pareto_sample
# Example usage:
result = pareto_sample(...)  # fill in arguments
```

- `triangular_sample(n)`
```python
from code.mystery import triangular_sample
# Example usage:
result = triangular_sample(...)  # fill in arguments
```

- `uniform_sample(n)`
```python
from code.mystery import uniform_sample
# Example usage:
result = uniform_sample(...)  # fill in arguments
```

- `weibull_sample(n)`
```python
from code.mystery import weibull_sample
# Example usage:
result = weibull_sample(...)  # fill in arguments
```

- `write_sample(sample, filename)`

Write a sequence of floats to a file.
    

```python
from code.mystery import write_sample
# Example usage:
result = write_sample(...)  # fill in arguments
```
